//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------

#include "SmartINJoystickNavigationAcePortFactory.hh"

// create a static instance of the default AcePortFactory
static SmartINJoystickNavigationAcePortFactory acePortFactory;

SmartINJoystickNavigationAcePortFactory::SmartINJoystickNavigationAcePortFactory()
{  
	componentImpl = 0;
	SmartINJoystickNavigation::instance()->addPortFactory("ACE_SmartSoft", this);
}

SmartINJoystickNavigationAcePortFactory::~SmartINJoystickNavigationAcePortFactory()
{  }

void SmartINJoystickNavigationAcePortFactory::initialize(SmartINJoystickNavigation *component, int argc, char* argv[])
{
	if(component->connections.component.defaultScheduler != "DEFAULT") {
		ACE_Sched_Params sched_params(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(component->connections.component.defaultScheduler == "FIFO") {
			sched_params.policy(ACE_SCHED_FIFO);
			sched_params.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(component->connections.component.defaultScheduler == "RR") {
			sched_params.policy(ACE_SCHED_RR);
			sched_params.priority(ACE_THR_PRI_RR_MIN);
		}
		// create new instance of the SmartSoft component with customized scheuling parameters 
		componentImpl = new SmartINJoystickNavigationImpl(component->connections.component.name, argc, argv, sched_params);
	} else {
		// create new instance of the SmartSoft component
		componentImpl = new SmartINJoystickNavigationImpl(component->connections.component.name, argc, argv);
	}
}

int SmartINJoystickNavigationAcePortFactory::onStartup()
{
	return componentImpl->startComponentInfrastructure();
}

Smart::IPushClientPattern<SmartInstitutionsServiceRepository::EnforcementInstructionPackage> * SmartINJoystickNavigationAcePortFactory::createEnforcementInstruction_Push()
{
	return new SmartACE::PushClient<SmartInstitutionsServiceRepository::EnforcementInstructionPackage>(componentImpl);
}

Smart::ISendClientPattern<SmartInstitutionsServiceRepository::EnforcementReplyPackage> * SmartINJoystickNavigationAcePortFactory::createEnforcementReply_Send()
{
	return new SmartACE::SendClient<SmartInstitutionsServiceRepository::EnforcementReplyPackage>(componentImpl);
}

Smart::IPushClientPattern<CommBasicObjects::CommJoystick> * SmartINJoystickNavigationAcePortFactory::createJoystickServiceIn()
{
	return new SmartACE::PushClient<CommBasicObjects::CommJoystick>(componentImpl);
}

Smart::ISendClientPattern<CommBasicObjects::CommNavigationVelocity> * SmartINJoystickNavigationAcePortFactory::createNavVelServiceOut()
{
	return new SmartACE::SendClient<CommBasicObjects::CommNavigationVelocity>(componentImpl);
}



SmartACE::SmartComponent* SmartINJoystickNavigationAcePortFactory::getComponentImpl()
{
	return componentImpl;
}

int SmartINJoystickNavigationAcePortFactory::onShutdown(const std::chrono::steady_clock::duration &timeoutTime)
{
	componentImpl->stopComponentInfrastructure(timeoutTime);
	return 0;
}

void SmartINJoystickNavigationAcePortFactory::destroy()
{
	// clean-up component's internally used resources (internally used communication middleware) 
	componentImpl->cleanUpComponentResources();
	delete componentImpl;
}
