//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "Enforcement_Query_HandlerCore.hh"
#include "Enforcement_Query_Handler.hh"

// include observers

Enforcement_Query_HandlerCore::Enforcement_Query_HandlerCore(IQueryServer* server)
:	Smart::IInputHandler<std::pair<Smart::QueryIdPtr,SmartInstitutionsServiceRepository::EnforcementInstructionPackage>>(server)
,	server(server)
{
}

Enforcement_Query_HandlerCore::~Enforcement_Query_HandlerCore()
{
}


void Enforcement_Query_HandlerCore::updateAllCommObjects()
{
}

void Enforcement_Query_HandlerCore::notify_all_interaction_observers() {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	// try dynamically down-casting this class to the derived class 
	// (we can do it safely here as we exactly know the derived class)
	if(const Enforcement_Query_Handler* enforcement_Query_Handler = dynamic_cast<const Enforcement_Query_Handler*>(this)) {
		for(auto it=interaction_observers.begin(); it!=interaction_observers.end(); it++) {
			(*it)->on_update_from(enforcement_Query_Handler);
		}
	}
}

void Enforcement_Query_HandlerCore::attach_interaction_observer(Enforcement_Query_HandlerObserverInterface *observer) {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	interaction_observers.push_back(observer);
}

void Enforcement_Query_HandlerCore::detach_interaction_observer(Enforcement_Query_HandlerObserverInterface *observer) {
	std::unique_lock<std::mutex> lock(interaction_observers_mutex);
	interaction_observers.remove(observer);
}
