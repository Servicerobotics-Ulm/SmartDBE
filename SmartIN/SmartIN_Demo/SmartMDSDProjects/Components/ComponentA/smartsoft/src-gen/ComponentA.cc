//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "ComponentA.hh"
#include "smartTimedTaskTrigger.h"
//FIXME: implement logging
//#include "smartGlobalLogger.hh"

// the ace port-factory is used as a default port-mapping
#include "ComponentAAcePortFactory.hh"

#include "Event_CreatorEventTestHandler.hh"

// initialize static singleton pointer to zero
ComponentA* ComponentA::_componentA = 0;

// constructor
ComponentA::ComponentA()
{
	std::cout << "constructor of ComponentA\n";
	
	// set all pointer members to NULL
	complianceManager = NULL;
	complianceManagerTrigger = NULL;
	componentActivity = NULL;
	componentActivityTrigger = NULL;
	enforcementInstruction_Push = NULL;
	enforcementInstruction_PushInputTaskTrigger = NULL;
	enforcementInstruction_PushUpcallManager = NULL;
	enforcementInstruction_Send = NULL;
	enforcementInstruction_SendInputTaskTrigger = NULL;
	enforcementInstruction_SendUpcallManager = NULL;
	enforcementReply_Push = NULL;
	enforcementReply_Send = NULL;
	enforcementReport_Push = NULL;
	enforcementReport_Send = NULL;
	enforcement_QueryReply = NULL;
	enforcement_QueryReplyInputTaskTrigger = NULL;
	enforcement_Query_Handler = NULL;
	eventManager = NULL;
	eventManagerTrigger = NULL;
	event_Creator = NULL;
	event_CreatorEventTestHandler = nullptr; 
	event_Listener = NULL;
	event_ListenerInputTaskTrigger = NULL;
	event_ListenerUpcallManager = NULL;
	report_QueryReply = NULL;
	report_QueryReplyInputTaskTrigger = NULL;
	report_Query_Handler = NULL;
	trafficLightsServiceOut = NULL;
	stateChangeHandler = NULL;
	stateSlave = NULL;
	wiringSlave = NULL;
	
	// set default ini parameter values
	connections.component.name = "ComponentA";
	connections.component.initialComponentMode = "Neutral";
	connections.component.defaultScheduler = "DEFAULT";
	connections.component.useLogger = false;
	
	connections.enforcementInstruction_Send.serviceName = "EnforcementInstruction_Send";
	connections.enforcementInstruction_Send.roboticMiddleware = "ACE_SmartSoft";
	connections.enforcementReply_Push.serviceName = "EnforcementReply_Push";
	connections.enforcementReply_Push.roboticMiddleware = "ACE_SmartSoft";
	connections.enforcementReport_Push.serviceName = "EnforcementReport_Push";
	connections.enforcementReport_Push.roboticMiddleware = "ACE_SmartSoft";
	connections.enforcement_QueryReply.serviceName = "Enforcement_QueryReply";
	connections.enforcement_QueryReply.roboticMiddleware = "ACE_SmartSoft";
	connections.event_Creator.serviceName = "Event_Creator";
	connections.event_Creator.roboticMiddleware = "ACE_SmartSoft";
	connections.report_QueryReply.serviceName = "Report_QueryReply";
	connections.report_QueryReply.roboticMiddleware = "ACE_SmartSoft";
	connections.trafficLightsServiceOut.serviceName = "TrafficLightsServiceOut";
	connections.trafficLightsServiceOut.roboticMiddleware = "ACE_SmartSoft";
	connections.enforcementInstruction_Push.wiringName = "EnforcementInstruction_Push";
	connections.enforcementInstruction_Push.serverName = "unknown";
	connections.enforcementInstruction_Push.serviceName = "unknown";
	connections.enforcementInstruction_Push.interval = 1;
	connections.enforcementInstruction_Push.roboticMiddleware = "ACE_SmartSoft";
	connections.enforcementReply_Send.initialConnect = false;
	connections.enforcementReply_Send.wiringName = "EnforcementReply_Send";
	connections.enforcementReply_Send.serverName = "unknown";
	connections.enforcementReply_Send.serviceName = "unknown";
	connections.enforcementReply_Send.interval = 1;
	connections.enforcementReply_Send.roboticMiddleware = "ACE_SmartSoft";
	connections.enforcementReport_Send.initialConnect = false;
	connections.enforcementReport_Send.wiringName = "EnforcementReport_Send";
	connections.enforcementReport_Send.serverName = "unknown";
	connections.enforcementReport_Send.serviceName = "unknown";
	connections.enforcementReport_Send.interval = 1;
	connections.enforcementReport_Send.roboticMiddleware = "ACE_SmartSoft";
	connections.event_Listener.wiringName = "Event_Listener";
	connections.event_Listener.serverName = "unknown";
	connections.event_Listener.serviceName = "unknown";
	connections.event_Listener.interval = 1;
	connections.event_Listener.roboticMiddleware = "ACE_SmartSoft";
	connections.complianceManager.minActFreq = 0.0;
	connections.complianceManager.maxActFreq = 0.0;
	// scheduling default parameters
	connections.complianceManager.scheduler = "DEFAULT";
	connections.complianceManager.priority = -1;
	connections.complianceManager.cpuAffinity = -1;
	connections.componentActivity.minActFreq = 0.0;
	connections.componentActivity.maxActFreq = 0.0;
	// scheduling default parameters
	connections.componentActivity.scheduler = "DEFAULT";
	connections.componentActivity.priority = -1;
	connections.componentActivity.cpuAffinity = -1;
	connections.eventManager.minActFreq = 0.0;
	connections.eventManager.maxActFreq = 0.0;
	// scheduling default parameters
	connections.eventManager.scheduler = "DEFAULT";
	connections.eventManager.priority = -1;
	connections.eventManager.cpuAffinity = -1;
	
	// initialize members of PlainOpcUaComponentAExtension
	
}

void ComponentA::addPortFactory(const std::string &name, ComponentAPortFactoryInterface *portFactory)
{
	portFactoryRegistry[name] = portFactory;
}

void ComponentA::addExtension(ComponentAExtension *extension)
{
	componentExtensionRegistry[extension->getName()] = extension;
}

SmartACE::SmartComponent* ComponentA::getComponentImpl()
{
	return dynamic_cast<ComponentAAcePortFactory*>(portFactoryRegistry["ACE_SmartSoft"])->getComponentImpl();
}

/**
 * Notify the component that setup/initialization is finished.
 * You may call this function from anywhere in the component.
 *
 * Set component's internal lifecycle state automaton (if any) into 
 * Alive mode (from here on the component is ready to provide its services)
 */
void ComponentA::setStartupFinished() {
	stateSlave->setWaitState("Alive");
	std::cout << "ComponentDefinition initialization/startup finished." << std::endl;
}


Smart::StatusCode ComponentA::connectEnforcementInstruction_Push(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = enforcementInstruction_Push->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->enforcementInstruction_Push->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	enforcementInstruction_Push->subscribe(connections.enforcementInstruction_Push.interval);
	return status;
}
Smart::StatusCode ComponentA::connectEnforcementReply_Send(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	if(connections.enforcementReply_Send.initialConnect == false) {
		return Smart::SMART_OK;
	}
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = enforcementReply_Send->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->enforcementReply_Send->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	return status;
}
Smart::StatusCode ComponentA::connectEnforcementReport_Send(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	if(connections.enforcementReport_Send.initialConnect == false) {
		return Smart::SMART_OK;
	}
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = enforcementReport_Send->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->enforcementReport_Send->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	return status;
}
Smart::StatusCode ComponentA::connectEvent_Listener(const std::string &serverName, const std::string &serviceName) {
	Smart::StatusCode status;
	
	std::cout << "connecting to: " << serverName << "; " << serviceName << std::endl;
	status = event_Listener->connect(serverName, serviceName);
	while(status != Smart::SMART_OK)
	{
		ACE_OS::sleep(ACE_Time_Value(0,500000));
		status = COMP->event_Listener->connect(serverName, serviceName);
	}
	std::cout << "connected.\n";
	return status;
}


/**
 * First connect ALL client ports contained in this component, then start all services:
 * activate state, push, etc...
 */
Smart::StatusCode ComponentA::connectAndStartAllServices() {
	Smart::StatusCode status = Smart::SMART_OK;
	
	status = connectEnforcementInstruction_Push(connections.enforcementInstruction_Push.serverName, connections.enforcementInstruction_Push.serviceName);
	if(status != Smart::SMART_OK) return status;
	status = connectEnforcementReply_Send(connections.enforcementReply_Send.serverName, connections.enforcementReply_Send.serviceName);
	if(status != Smart::SMART_OK) return status;
	status = connectEnforcementReport_Send(connections.enforcementReport_Send.serverName, connections.enforcementReport_Send.serviceName);
	if(status != Smart::SMART_OK) return status;
	status = connectEvent_Listener(connections.event_Listener.serverName, connections.event_Listener.serviceName);
	if(status != Smart::SMART_OK) return status;
	return status;
}

/**
 * Start all tasks contained in this component.
 */
void ComponentA::startAllTasks() {
	// start task ComplianceManager
	if(connections.complianceManager.scheduler != "DEFAULT") {
		ACE_Sched_Params complianceManager_SchedParams(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(connections.complianceManager.scheduler == "FIFO") {
			complianceManager_SchedParams.policy(ACE_SCHED_FIFO);
			complianceManager_SchedParams.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(connections.complianceManager.scheduler == "RR") {
			complianceManager_SchedParams.policy(ACE_SCHED_RR);
			complianceManager_SchedParams.priority(ACE_THR_PRI_RR_MIN);
		}
		complianceManager->start(complianceManager_SchedParams, connections.complianceManager.cpuAffinity);
	} else {
		complianceManager->start();
	}
	// start task ComponentActivity
	if(connections.componentActivity.scheduler != "DEFAULT") {
		ACE_Sched_Params componentActivity_SchedParams(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(connections.componentActivity.scheduler == "FIFO") {
			componentActivity_SchedParams.policy(ACE_SCHED_FIFO);
			componentActivity_SchedParams.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(connections.componentActivity.scheduler == "RR") {
			componentActivity_SchedParams.policy(ACE_SCHED_RR);
			componentActivity_SchedParams.priority(ACE_THR_PRI_RR_MIN);
		}
		componentActivity->start(componentActivity_SchedParams, connections.componentActivity.cpuAffinity);
	} else {
		componentActivity->start();
	}
	// start task EventManager
	if(connections.eventManager.scheduler != "DEFAULT") {
		ACE_Sched_Params eventManager_SchedParams(ACE_SCHED_OTHER, ACE_THR_PRI_OTHER_DEF);
		if(connections.eventManager.scheduler == "FIFO") {
			eventManager_SchedParams.policy(ACE_SCHED_FIFO);
			eventManager_SchedParams.priority(ACE_THR_PRI_FIFO_MIN);
		} else if(connections.eventManager.scheduler == "RR") {
			eventManager_SchedParams.policy(ACE_SCHED_RR);
			eventManager_SchedParams.priority(ACE_THR_PRI_RR_MIN);
		}
		eventManager->start(eventManager_SchedParams, connections.eventManager.cpuAffinity);
	} else {
		eventManager->start();
	}
}

/**
 * Start all timers contained in this component
 */
void ComponentA::startAllTimers() {
}


Smart::TaskTriggerSubject* ComponentA::getInputTaskTriggerFromString(const std::string &client)
{
	if(client == "EnforcementInstruction_Push") return enforcementInstruction_PushInputTaskTrigger;
	if(client == "EnforcementInstruction_Send") return enforcementInstruction_SendInputTaskTrigger;
	if(client == "Event_Listener") return event_ListenerInputTaskTrigger;
	
	return NULL;
}


void ComponentA::init(int argc, char *argv[])
{
	try {
		Smart::StatusCode status;
		
		// load initial parameters from ini-file (if found)
		loadParameter(argc, argv);
		
		
		// initializations of PlainOpcUaComponentAExtension
		
		
		// initialize all registered port-factories
		for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
		{
			portFactory->second->initialize(this, argc, argv);
		}
		
		// initialize all registered component-extensions
		for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
		{
			extension->second->initialize(this, argc, argv);
		}
		
		ComponentAPortFactoryInterface *acePortFactory = portFactoryRegistry["ACE_SmartSoft"];
		if(acePortFactory == 0) {
			std::cerr << "ERROR: acePortFactory NOT instantiated -> exit(-1)" << std::endl;
			exit(-1);
		}
		
		// this pointer is used for backwards compatibility (deprecated: should be removed as soon as all patterns, including coordination, are moved to port-factory)
		SmartACE::SmartComponent *component = dynamic_cast<ComponentAAcePortFactory*>(acePortFactory)->getComponentImpl();
		
		std::cout << "ComponentDefinition ComponentA is named " << connections.component.name << std::endl;
		
		if(connections.component.useLogger == true) {
			//FIXME: use logging
			//Smart::LOGGER->openLogFileInFolder("data/"+connections.component.name);
			//Smart::LOGGER->startLogging();
		}

		// create event-test handlers (if needed)
		event_CreatorEventTestHandler = std::make_shared<Event_CreatorEventTestHandler>();
		
		// create server ports
		// TODO: set minCycleTime from Ini-file
		enforcementInstruction_Send = portFactoryRegistry[connections.enforcementInstruction_Send.roboticMiddleware]->createEnforcementInstruction_Send(connections.enforcementInstruction_Send.serviceName);
		enforcementReply_Push = portFactoryRegistry[connections.enforcementReply_Push.roboticMiddleware]->createEnforcementReply_Push(connections.enforcementReply_Push.serviceName);
		enforcementReport_Push = portFactoryRegistry[connections.enforcementReport_Push.roboticMiddleware]->createEnforcementReport_Push(connections.enforcementReport_Push.serviceName);
		enforcement_QueryReply = portFactoryRegistry[connections.enforcement_QueryReply.roboticMiddleware]->createEnforcement_QueryReply(connections.enforcement_QueryReply.serviceName);
		enforcement_QueryReplyInputTaskTrigger = new Smart::QueryServerTaskTrigger<SmartInstitutions_ServiceRepository::EnforcementInstructionPackage, SmartInstitutions_ServiceRepository::EnforcementReplyPackage>(enforcement_QueryReply);
		event_CreatorEventTestHandler = std::make_shared<Event_CreatorEventTestHandler>();
		event_Creator = portFactoryRegistry[connections.event_Creator.roboticMiddleware]->createEvent_Creator(connections.event_Creator.serviceName, event_CreatorEventTestHandler);
		report_QueryReply = portFactoryRegistry[connections.report_QueryReply.roboticMiddleware]->createReport_QueryReply(connections.report_QueryReply.serviceName);
		report_QueryReplyInputTaskTrigger = new Smart::QueryServerTaskTrigger<SmartInstitutions_ServiceRepository::MemberIdentifier, SmartInstitutions_ServiceRepository::EnforcementReportPackage>(report_QueryReply);
		trafficLightsServiceOut = portFactoryRegistry[connections.trafficLightsServiceOut.roboticMiddleware]->createTrafficLightsServiceOut(connections.trafficLightsServiceOut.serviceName);
		
		// create client ports
		enforcementInstruction_Push = portFactoryRegistry[connections.enforcementInstruction_Push.roboticMiddleware]->createEnforcementInstruction_Push();
		enforcementReply_Send = portFactoryRegistry[connections.enforcementReply_Send.roboticMiddleware]->createEnforcementReply_Send();
		enforcementReport_Send = portFactoryRegistry[connections.enforcementReport_Send.roboticMiddleware]->createEnforcementReport_Send();
		event_Listener = portFactoryRegistry[connections.event_Listener.roboticMiddleware]->createEvent_Listener();
		
		// create InputTaskTriggers and UpcallManagers
		enforcementInstruction_PushInputTaskTrigger = new Smart::InputTaskTrigger<SmartInstitutions_ServiceRepository::EnforcementInstructionPackage>(enforcementInstruction_Push);
		enforcementInstruction_PushUpcallManager = new EnforcementInstruction_PushUpcallManager(enforcementInstruction_Push);
		enforcementInstruction_SendInputTaskTrigger = new Smart::InputTaskTrigger<SmartInstitutions_ServiceRepository::EnforcementInstructionPackage>(enforcementInstruction_Send);
		enforcementInstruction_SendUpcallManager = new EnforcementInstruction_SendUpcallManager(enforcementInstruction_Send);
		event_ListenerInputTaskTrigger = new Smart::InputTaskTrigger<Smart::EventInputType<SmartInstitutions_ServiceRepository::SmartIN_EventType>>(event_Listener);
		event_ListenerUpcallManager = new Event_ListenerUpcallManager(event_Listener);
		
		// create input-handler
		
		// create request-handlers
		enforcement_Query_Handler = new Enforcement_Query_Handler(enforcement_QueryReply);
		report_Query_Handler = new Report_Query_Handler(report_QueryReply);
		
		// create state pattern
		stateChangeHandler = new SmartStateChangeHandler();
		stateSlave = new SmartACE::StateSlave(component, stateChangeHandler);
		status = stateSlave->setUpInitialState(connections.component.initialComponentMode);
		if (status != Smart::SMART_OK) std::cerr << status << "; failed setting initial ComponentMode: " << connections.component.initialComponentMode << std::endl;
		// activate state slave
		status = stateSlave->activate();
		if(status != Smart::SMART_OK) std::cerr << "ERROR: activate state" << std::endl;
		
		wiringSlave = new SmartACE::WiringSlave(component);
		// add client port to wiring slave
		if(connections.enforcementInstruction_Push.roboticMiddleware == "ACE_SmartSoft") {
			//FIXME: this must also work with other implementations
			dynamic_cast<SmartACE::PushClient<SmartInstitutions_ServiceRepository::EnforcementInstructionPackage>*>(enforcementInstruction_Push)->add(wiringSlave, connections.enforcementInstruction_Push.wiringName);
		}
		if(connections.enforcementReply_Send.roboticMiddleware == "ACE_SmartSoft") {
			//FIXME: this must also work with other implementations
			dynamic_cast<SmartACE::SendClient<SmartInstitutions_ServiceRepository::EnforcementReplyPackage>*>(enforcementReply_Send)->add(wiringSlave, connections.enforcementReply_Send.wiringName);
		}
		if(connections.enforcementReport_Send.roboticMiddleware == "ACE_SmartSoft") {
			//FIXME: this must also work with other implementations
			dynamic_cast<SmartACE::SendClient<SmartInstitutions_ServiceRepository::EnforcementReportPackage>*>(enforcementReport_Send)->add(wiringSlave, connections.enforcementReport_Send.wiringName);
		}
		if(connections.event_Listener.roboticMiddleware == "ACE_SmartSoft") {
			//FIXME: this must also work with other implementations
			dynamic_cast<SmartACE::EventClient<SmartInstitutions_ServiceRepository::SmartIN_Command, SmartInstitutions_ServiceRepository::SmartIN_EventType>*>(event_Listener)->add(wiringSlave, connections.event_Listener.wiringName);
		}
		
		
		
		// create Task ComplianceManager
		complianceManager = new ComplianceManager(component);
		// configure input-links
		enforcementInstruction_PushUpcallManager->attach(complianceManager);
		enforcementInstruction_SendUpcallManager->attach(complianceManager);
		// configure task-trigger (if task is configurable)
		if(connections.complianceManager.trigger == "PeriodicTimer") {
			// create PeriodicTimerTrigger
			int microseconds = 1000*1000 / connections.complianceManager.periodicActFreq;
			if(microseconds > 0) {
				Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
				triggerPtr->attach(complianceManager);
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				// store trigger in class member
				complianceManagerTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task ComplianceManager" << std::endl;
			}
		} else if(connections.complianceManager.trigger == "DataTriggered") {
			complianceManagerTrigger = getInputTaskTriggerFromString(connections.complianceManager.inPortRef);
			if(complianceManagerTrigger != NULL) {
				complianceManagerTrigger->attach(complianceManager, connections.complianceManager.prescale);
			} else {
				std::cerr << "ERROR: could not set-up InPort " << connections.complianceManager.inPortRef << " as activation source for Task ComplianceManager" << std::endl;
			}
		} 
		
		// create Task ComponentActivity
		componentActivity = new ComponentActivity(component);
		// configure input-links
		// configure task-trigger (if task is configurable)
		if(connections.componentActivity.trigger == "PeriodicTimer") {
			// create PeriodicTimerTrigger
			int microseconds = 1000*1000 / connections.componentActivity.periodicActFreq;
			if(microseconds > 0) {
				Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
				triggerPtr->attach(componentActivity);
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				// store trigger in class member
				componentActivityTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task ComponentActivity" << std::endl;
			}
		} else if(connections.componentActivity.trigger == "DataTriggered") {
			componentActivityTrigger = getInputTaskTriggerFromString(connections.componentActivity.inPortRef);
			if(componentActivityTrigger != NULL) {
				componentActivityTrigger->attach(componentActivity, connections.componentActivity.prescale);
			} else {
				std::cerr << "ERROR: could not set-up InPort " << connections.componentActivity.inPortRef << " as activation source for Task ComponentActivity" << std::endl;
			}
		} 
		
		// create Task EventManager
		eventManager = new EventManager(component);
		// configure input-links
		event_ListenerUpcallManager->attach(eventManager);
		// configure task-trigger (if task is configurable)
		if(connections.eventManager.trigger == "PeriodicTimer") {
			// create PeriodicTimerTrigger
			int microseconds = 1000*1000 / connections.eventManager.periodicActFreq;
			if(microseconds > 0) {
				Smart::TimedTaskTrigger *triggerPtr = new Smart::TimedTaskTrigger();
				triggerPtr->attach(eventManager);
				component->getTimerManager()->scheduleTimer(triggerPtr, (void *) 0, std::chrono::microseconds(microseconds), std::chrono::microseconds(microseconds));
				// store trigger in class member
				eventManagerTrigger = triggerPtr;
			} else {
				std::cerr << "ERROR: could not set-up Timer with cycle-time " << microseconds << " as activation source for Task EventManager" << std::endl;
			}
		} else if(connections.eventManager.trigger == "DataTriggered") {
			eventManagerTrigger = getInputTaskTriggerFromString(connections.eventManager.inPortRef);
			if(eventManagerTrigger != NULL) {
				eventManagerTrigger->attach(eventManager, connections.eventManager.prescale);
			} else {
				std::cerr << "ERROR: could not set-up InPort " << connections.eventManager.inPortRef << " as activation source for Task EventManager" << std::endl;
			}
		} 
		
		
		// link observers with subjects
	} catch (const std::exception &ex) {
		std::cerr << "Uncaught std exception" << ex.what() << std::endl;
	} catch (...) {
		std::cerr << "Uncaught exception" << std::endl;
	}
}

// run the component
void ComponentA::run()
{
	stateSlave->acquire("init");
	// startup all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->onStartup();
	}
	
	// startup all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->onStartup();
	}
	stateSlave->release("init");
	
	// do not call this handler within the init state (see above) as this handler internally calls setStartupFinished() (this should be fixed in future)
	compHandler.onStartup();
	
	// this call blocks until the component is commanded to shutdown
	stateSlave->acquire("shutdown");
	
	// shutdown all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->onShutdown();
	}
	
	// shutdown all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->onShutdown();
	}
	
	if(connections.component.useLogger == true) {
		//FIXME: use logging
		//Smart::LOGGER->stopLogging();
	}
	
	compHandler.onShutdown();
	
	stateSlave->release("shutdown");
}

// clean-up component's resources
void ComponentA::fini()
{
	// unlink all observers
	
	// destroy all task instances
	// unlink all UpcallManagers
	enforcementInstruction_PushUpcallManager->detach(complianceManager);
	enforcementInstruction_SendUpcallManager->detach(complianceManager);
	// unlink the TaskTrigger
	if(complianceManagerTrigger != NULL){
		complianceManagerTrigger->detach(complianceManager);
		delete complianceManager;
	}
	// unlink all UpcallManagers
	// unlink the TaskTrigger
	if(componentActivityTrigger != NULL){
		componentActivityTrigger->detach(componentActivity);
		delete componentActivity;
	}
	// unlink all UpcallManagers
	event_ListenerUpcallManager->detach(eventManager);
	// unlink the TaskTrigger
	if(eventManagerTrigger != NULL){
		eventManagerTrigger->detach(eventManager);
		delete eventManager;
	}

	// destroy all input-handler

	// destroy InputTaskTriggers and UpcallManagers
	delete enforcementInstruction_PushInputTaskTrigger;
	delete enforcementInstruction_PushUpcallManager;
	delete enforcementInstruction_SendInputTaskTrigger;
	delete enforcementInstruction_SendUpcallManager;
	delete event_ListenerInputTaskTrigger;
	delete event_ListenerUpcallManager;

	// destroy client ports
	delete enforcementInstruction_Push;
	delete enforcementReply_Send;
	delete enforcementReport_Send;
	delete event_Listener;

	// destroy server ports
	delete enforcementInstruction_Send;
	delete enforcementReply_Push;
	delete enforcementReport_Push;
	delete enforcement_QueryReply;
	delete enforcement_QueryReplyInputTaskTrigger;
	delete event_Creator;
	delete report_QueryReply;
	delete report_QueryReplyInputTaskTrigger;
	delete trafficLightsServiceOut;
	// destroy event-test handlers (if needed)
	event_CreatorEventTestHandler;
	
	// destroy request-handlers
	delete enforcement_Query_Handler;
	delete report_Query_Handler;
	
	delete stateSlave;
	// destroy state-change-handler
	delete stateChangeHandler;
	
	// destroy all master/slave ports
	delete wiringSlave;
	

	// destroy all registered component-extensions
	for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
	{
		extension->second->destroy();
	}

	// destroy all registered port-factories
	for(auto portFactory = portFactoryRegistry.begin(); portFactory != portFactoryRegistry.end(); portFactory++) 
	{
		portFactory->second->destroy();
	}
	
	// destruction of PlainOpcUaComponentAExtension
	
}

void ComponentA::loadParameter(int argc, char *argv[])
{
	/*
	 Parameters can be specified via command line --filename=<filename> or -f <filename>

	 With this parameter present:
	   - The component will look for the file in the current working directory,
	     a path relative to the current directory or any absolute path
	   - The component will use the default values if the file cannot be found

	 With this parameter absent:
	   - <Name of Component>.ini will be read from current working directory, if found there
	   - $SMART_ROOT/etc/<Name of Component>.ini will be read otherwise
	   - Default values will be used if neither found in working directory or /etc
	 */
	SmartACE::SmartIniParameter parameter;
	std::ifstream parameterfile;
	bool parameterFileFound = false;

	// load parameters
	try
	{
		// if paramfile is given as argument
		if(parameter.tryAddFileFromArgs(argc,argv,"filename", 'f'))
		{
			parameterFileFound = true;
			std::cout << "parameter file is loaded from an argv argument \n";
		} else if(parameter.searchFile("ComponentA.ini", parameterfile)) {
			parameterFileFound = true;
			std::cout << "load ComponentA.ini parameter file\n";
			parameter.addFile(parameterfile);
		} else {
			std::cout << "WARNING: ComponentA.ini parameter file not found! (using default values or command line arguments)\n";
		}
		
		// add command line arguments to allow overwriting of parameters
		// from file
		parameter.addCommandLineArgs(argc,argv,"component");
		
		// initialize the naming service using the command line parameters parsed in the
		// SmartIniParameter class. The naming service parameters are expected to be in
		// the "component" parameter group.
		SmartACE::NAMING::instance()->checkForHelpArg(argc,argv);
		if(parameterFileFound) 
		{
			if(SmartACE::NAMING::instance()->init(parameter.getAllParametersFromGroup("component")) != 0) {
				// initialization of naming service failed
				throw std::logic_error( "<NamingService> Service initialization failed!\nPossible causes could be:\n-> Erroneous configuration.\n-> Naming service not reachable.\n" );
			}
		} else {
			if(SmartACE::NAMING::instance()->init(argc, argv) != 0) {
				// initialization of naming service failed
				throw std::logic_error( "<NamingService> Service initialization failed!\nPossible causes could be:\n-> Erroneous configuration.\n-> Naming service not reachable.\n" );
			}
		}
			
		// print all known parameters
		// parameter.print();
		
		//--- server port // client port // other parameter ---
		// load parameter
		parameter.getString("component", "name", connections.component.name);
		parameter.getString("component", "initialComponentMode", connections.component.initialComponentMode);
		if(parameter.checkIfParameterExists("component", "defaultScheduler")) {
			parameter.getString("component", "defaultScheduler", connections.component.defaultScheduler);
		}
		if(parameter.checkIfParameterExists("component", "useLogger")) {
			parameter.getBoolean("component", "useLogger", connections.component.useLogger);
		}
		
		// load parameters for client EnforcementInstruction_Push
		parameter.getString("EnforcementInstruction_Push", "serviceName", connections.enforcementInstruction_Push.serviceName);
		parameter.getString("EnforcementInstruction_Push", "serverName", connections.enforcementInstruction_Push.serverName);
		parameter.getString("EnforcementInstruction_Push", "wiringName", connections.enforcementInstruction_Push.wiringName);
		parameter.getInteger("EnforcementInstruction_Push", "interval", connections.enforcementInstruction_Push.interval);
		if(parameter.checkIfParameterExists("EnforcementInstruction_Push", "roboticMiddleware")) {
			parameter.getString("EnforcementInstruction_Push", "roboticMiddleware", connections.enforcementInstruction_Push.roboticMiddleware);
		}
		// load parameters for client EnforcementReply_Send
		parameter.getBoolean("EnforcementReply_Send", "initialConnect", connections.enforcementReply_Send.initialConnect);
		parameter.getString("EnforcementReply_Send", "serviceName", connections.enforcementReply_Send.serviceName);
		parameter.getString("EnforcementReply_Send", "serverName", connections.enforcementReply_Send.serverName);
		parameter.getString("EnforcementReply_Send", "wiringName", connections.enforcementReply_Send.wiringName);
		if(parameter.checkIfParameterExists("EnforcementReply_Send", "roboticMiddleware")) {
			parameter.getString("EnforcementReply_Send", "roboticMiddleware", connections.enforcementReply_Send.roboticMiddleware);
		}
		// load parameters for client EnforcementReport_Send
		parameter.getBoolean("EnforcementReport_Send", "initialConnect", connections.enforcementReport_Send.initialConnect);
		parameter.getString("EnforcementReport_Send", "serviceName", connections.enforcementReport_Send.serviceName);
		parameter.getString("EnforcementReport_Send", "serverName", connections.enforcementReport_Send.serverName);
		parameter.getString("EnforcementReport_Send", "wiringName", connections.enforcementReport_Send.wiringName);
		if(parameter.checkIfParameterExists("EnforcementReport_Send", "roboticMiddleware")) {
			parameter.getString("EnforcementReport_Send", "roboticMiddleware", connections.enforcementReport_Send.roboticMiddleware);
		}
		// load parameters for client Event_Listener
		parameter.getString("Event_Listener", "serviceName", connections.event_Listener.serviceName);
		parameter.getString("Event_Listener", "serverName", connections.event_Listener.serverName);
		parameter.getString("Event_Listener", "wiringName", connections.event_Listener.wiringName);
		if(parameter.checkIfParameterExists("Event_Listener", "roboticMiddleware")) {
			parameter.getString("Event_Listener", "roboticMiddleware", connections.event_Listener.roboticMiddleware);
		}
		
		// load parameters for server EnforcementInstruction_Send
		parameter.getString("EnforcementInstruction_Send", "serviceName", connections.enforcementInstruction_Send.serviceName);
		if(parameter.checkIfParameterExists("EnforcementInstruction_Send", "roboticMiddleware")) {
			parameter.getString("EnforcementInstruction_Send", "roboticMiddleware", connections.enforcementInstruction_Send.roboticMiddleware);
		}
		// load parameters for server EnforcementReply_Push
		parameter.getString("EnforcementReply_Push", "serviceName", connections.enforcementReply_Push.serviceName);
		if(parameter.checkIfParameterExists("EnforcementReply_Push", "roboticMiddleware")) {
			parameter.getString("EnforcementReply_Push", "roboticMiddleware", connections.enforcementReply_Push.roboticMiddleware);
		}
		// load parameters for server EnforcementReport_Push
		parameter.getString("EnforcementReport_Push", "serviceName", connections.enforcementReport_Push.serviceName);
		if(parameter.checkIfParameterExists("EnforcementReport_Push", "roboticMiddleware")) {
			parameter.getString("EnforcementReport_Push", "roboticMiddleware", connections.enforcementReport_Push.roboticMiddleware);
		}
		// load parameters for server Enforcement_QueryReply
		parameter.getString("Enforcement_QueryReply", "serviceName", connections.enforcement_QueryReply.serviceName);
		if(parameter.checkIfParameterExists("Enforcement_QueryReply", "roboticMiddleware")) {
			parameter.getString("Enforcement_QueryReply", "roboticMiddleware", connections.enforcement_QueryReply.roboticMiddleware);
		}
		// load parameters for server Event_Creator
		parameter.getString("Event_Creator", "serviceName", connections.event_Creator.serviceName);
		if(parameter.checkIfParameterExists("Event_Creator", "roboticMiddleware")) {
			parameter.getString("Event_Creator", "roboticMiddleware", connections.event_Creator.roboticMiddleware);
		}
		// load parameters for server Report_QueryReply
		parameter.getString("Report_QueryReply", "serviceName", connections.report_QueryReply.serviceName);
		if(parameter.checkIfParameterExists("Report_QueryReply", "roboticMiddleware")) {
			parameter.getString("Report_QueryReply", "roboticMiddleware", connections.report_QueryReply.roboticMiddleware);
		}
		// load parameters for server TrafficLightsServiceOut
		parameter.getString("TrafficLightsServiceOut", "serviceName", connections.trafficLightsServiceOut.serviceName);
		if(parameter.checkIfParameterExists("TrafficLightsServiceOut", "roboticMiddleware")) {
			parameter.getString("TrafficLightsServiceOut", "roboticMiddleware", connections.trafficLightsServiceOut.roboticMiddleware);
		}
		
		// load parameters for task ComplianceManager
		parameter.getDouble("ComplianceManager", "minActFreqHz", connections.complianceManager.minActFreq);
		parameter.getDouble("ComplianceManager", "maxActFreqHz", connections.complianceManager.maxActFreq);
		parameter.getString("ComplianceManager", "triggerType", connections.complianceManager.trigger);
		if(connections.complianceManager.trigger == "PeriodicTimer") {
			parameter.getDouble("ComplianceManager", "periodicActFreqHz", connections.complianceManager.periodicActFreq);
		} else if(connections.complianceManager.trigger == "DataTriggered") {
			parameter.getString("ComplianceManager", "inPortRef", connections.complianceManager.inPortRef);
			parameter.getInteger("ComplianceManager", "prescale", connections.complianceManager.prescale);
		}
		if(parameter.checkIfParameterExists("ComplianceManager", "scheduler")) {
			parameter.getString("ComplianceManager", "scheduler", connections.complianceManager.scheduler);
		}
		if(parameter.checkIfParameterExists("ComplianceManager", "priority")) {
			parameter.getInteger("ComplianceManager", "priority", connections.complianceManager.priority);
		}
		if(parameter.checkIfParameterExists("ComplianceManager", "cpuAffinity")) {
			parameter.getInteger("ComplianceManager", "cpuAffinity", connections.complianceManager.cpuAffinity);
		}
		// load parameters for task ComponentActivity
		parameter.getDouble("ComponentActivity", "minActFreqHz", connections.componentActivity.minActFreq);
		parameter.getDouble("ComponentActivity", "maxActFreqHz", connections.componentActivity.maxActFreq);
		parameter.getString("ComponentActivity", "triggerType", connections.componentActivity.trigger);
		if(connections.componentActivity.trigger == "PeriodicTimer") {
			parameter.getDouble("ComponentActivity", "periodicActFreqHz", connections.componentActivity.periodicActFreq);
		} else if(connections.componentActivity.trigger == "DataTriggered") {
			parameter.getString("ComponentActivity", "inPortRef", connections.componentActivity.inPortRef);
			parameter.getInteger("ComponentActivity", "prescale", connections.componentActivity.prescale);
		}
		if(parameter.checkIfParameterExists("ComponentActivity", "scheduler")) {
			parameter.getString("ComponentActivity", "scheduler", connections.componentActivity.scheduler);
		}
		if(parameter.checkIfParameterExists("ComponentActivity", "priority")) {
			parameter.getInteger("ComponentActivity", "priority", connections.componentActivity.priority);
		}
		if(parameter.checkIfParameterExists("ComponentActivity", "cpuAffinity")) {
			parameter.getInteger("ComponentActivity", "cpuAffinity", connections.componentActivity.cpuAffinity);
		}
		// load parameters for task EventManager
		parameter.getDouble("EventManager", "minActFreqHz", connections.eventManager.minActFreq);
		parameter.getDouble("EventManager", "maxActFreqHz", connections.eventManager.maxActFreq);
		parameter.getString("EventManager", "triggerType", connections.eventManager.trigger);
		if(connections.eventManager.trigger == "PeriodicTimer") {
			parameter.getDouble("EventManager", "periodicActFreqHz", connections.eventManager.periodicActFreq);
		} else if(connections.eventManager.trigger == "DataTriggered") {
			parameter.getString("EventManager", "inPortRef", connections.eventManager.inPortRef);
			parameter.getInteger("EventManager", "prescale", connections.eventManager.prescale);
		}
		if(parameter.checkIfParameterExists("EventManager", "scheduler")) {
			parameter.getString("EventManager", "scheduler", connections.eventManager.scheduler);
		}
		if(parameter.checkIfParameterExists("EventManager", "priority")) {
			parameter.getInteger("EventManager", "priority", connections.eventManager.priority);
		}
		if(parameter.checkIfParameterExists("EventManager", "cpuAffinity")) {
			parameter.getInteger("EventManager", "cpuAffinity", connections.eventManager.cpuAffinity);
		}
		
		// load parameters for PlainOpcUaComponentAExtension
		
		
		// load parameters for all registered component-extensions
		for(auto extension = componentExtensionRegistry.begin(); extension != componentExtensionRegistry.end(); extension++) 
		{
			extension->second->loadParameters(parameter);
		}
		
	
	} catch (const SmartACE::IniParameterError & e) {
		std::cerr << e.what() << std::endl;
	} catch (const std::exception &ex) {
		std::cerr << "Uncaught std::exception: " << ex.what() << std::endl;
	} catch (...) {
		std::cerr << "Uncaught exception" << std::endl;
	}
}
