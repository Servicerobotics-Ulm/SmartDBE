//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "SmartInstitutions_ServiceRepository/SmartIN_EventMessageCore.hh"

// serialization/deserialization operators
//#include "SmartInstitutions_ServiceRepository/SmartIN_EventMessageACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace SmartInstitutions_ServiceRepository 
{
	const char* SmartIN_EventMessageCore::getCompiledHash()
	{
		return SmartInstitutions_ServiceRepositoryIDL::REPO_HASH;
	}
	
	void SmartIN_EventMessageCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for SmartInstitutions_ServiceRepository::EnforcementInstructionPackage(idl_SmartIN_EventMessage.ifEnforcementInstructionPackage)
		SmartInstitutions_ServiceRepository::EnforcementInstructionPackage::getAllHashValues(hashes);
		// get hash value(s) for SmartInstitutions_ServiceRepository::EnforcementReplyPackage(idl_SmartIN_EventMessage.ifEnforcementReplyPackage)
		SmartInstitutions_ServiceRepository::EnforcementReplyPackage::getAllHashValues(hashes);
		// get hash value(s) for SmartInstitutions_ServiceRepository::EnforcementReportPackage(idl_SmartIN_EventMessage.ifEnforcementReportPackage)
		SmartInstitutions_ServiceRepository::EnforcementReportPackage::getAllHashValues(hashes);
	}
	
	void SmartIN_EventMessageCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "SmartIN_EventMessageCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for SmartInstitutions_ServiceRepository::EnforcementInstructionPackage(idl_SmartIN_EventMessage.ifEnforcementInstructionPackage)
		SmartInstitutions_ServiceRepository::EnforcementInstructionPackage::checkAllHashValues(hashes);
		// check hash value(s) for SmartInstitutions_ServiceRepository::EnforcementReplyPackage(idl_SmartIN_EventMessage.ifEnforcementReplyPackage)
		SmartInstitutions_ServiceRepository::EnforcementReplyPackage::checkAllHashValues(hashes);
		// check hash value(s) for SmartInstitutions_ServiceRepository::EnforcementReportPackage(idl_SmartIN_EventMessage.ifEnforcementReportPackage)
		SmartInstitutions_ServiceRepository::EnforcementReportPackage::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t SmartIN_EventMessageCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, data.usetype);
		seed += SmartInstitutions_ServiceRepository::EnforcementInstructionPackage::generateDataHash(data.ifEnforcementInstructionPackage);
		seed += SmartInstitutions_ServiceRepository::EnforcementReplyPackage::generateDataHash(data.ifEnforcementReplyPackage);
		seed += SmartInstitutions_ServiceRepository::EnforcementReportPackage::generateDataHash(data.ifEnforcementReportPackage);
		
		return seed;
	}
	#endif
	
	// default constructor
	SmartIN_EventMessageCore::SmartIN_EventMessageCore()
	:	idl_SmartIN_EventMessage()
	{  
		setUsetype(SmartInstitutions_ServiceRepository::SmartINPackageTypeEnums());
		setIfEnforcementInstructionPackage(SmartInstitutions_ServiceRepository::EnforcementInstructionPackage());
		setIfEnforcementReplyPackage(SmartInstitutions_ServiceRepository::EnforcementReplyPackage());
		setIfEnforcementReportPackage(SmartInstitutions_ServiceRepository::EnforcementReportPackage());
	}
	
	SmartIN_EventMessageCore::SmartIN_EventMessageCore(const DATATYPE &data)
	:	idl_SmartIN_EventMessage(data)
	{  }

	SmartIN_EventMessageCore::~SmartIN_EventMessageCore()
	{  }
	
	void SmartIN_EventMessageCore::to_ostream(std::ostream &os) const
	{
	  os << "SmartIN_EventMessage(";
	  getUsetype().to_ostream(os);
	  getIfEnforcementInstructionPackage().to_ostream(os);
	  getIfEnforcementReplyPackage().to_ostream(os);
	  getIfEnforcementReportPackage().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void SmartIN_EventMessageCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<usetype>";
		getUsetype().to_xml(os, indent);
		os << indent << "</usetype>";
		os << indent << "<ifEnforcementInstructionPackage>";
		getIfEnforcementInstructionPackage().to_xml(os, indent);
		os << indent << "</ifEnforcementInstructionPackage>";
		os << indent << "<ifEnforcementReplyPackage>";
		getIfEnforcementReplyPackage().to_xml(os, indent);
		os << indent << "</ifEnforcementReplyPackage>";
		os << indent << "<ifEnforcementReportPackage>";
		getIfEnforcementReportPackage().to_xml(os, indent);
		os << indent << "</ifEnforcementReportPackage>";
	}
	
	// restore from xml stream
	void SmartIN_EventMessageCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_usetype("<usetype>");
		static const Smart::KnuthMorrisPratt kmp_ifEnforcementInstructionPackage("<ifEnforcementInstructionPackage>");
		static const Smart::KnuthMorrisPratt kmp_ifEnforcementReplyPackage("<ifEnforcementReplyPackage>");
		static const Smart::KnuthMorrisPratt kmp_ifEnforcementReportPackage("<ifEnforcementReportPackage>");
		
		if(kmp_usetype.search(is)) {
			SmartInstitutions_ServiceRepository::SmartINPackageTypeEnums usetypeItem;
			usetypeItem.from_xml(is);
			setUsetype(usetypeItem);
		}
		if(kmp_ifEnforcementInstructionPackage.search(is)) {
			SmartInstitutions_ServiceRepository::EnforcementInstructionPackage ifEnforcementInstructionPackageItem;
			ifEnforcementInstructionPackageItem.from_xml(is);
			setIfEnforcementInstructionPackage(ifEnforcementInstructionPackageItem);
		}
		if(kmp_ifEnforcementReplyPackage.search(is)) {
			SmartInstitutions_ServiceRepository::EnforcementReplyPackage ifEnforcementReplyPackageItem;
			ifEnforcementReplyPackageItem.from_xml(is);
			setIfEnforcementReplyPackage(ifEnforcementReplyPackageItem);
		}
		if(kmp_ifEnforcementReportPackage.search(is)) {
			SmartInstitutions_ServiceRepository::EnforcementReportPackage ifEnforcementReportPackageItem;
			ifEnforcementReportPackageItem.from_xml(is);
			setIfEnforcementReportPackage(ifEnforcementReportPackageItem);
		}
	}
	
	/*
	void SmartIN_EventMessageCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		SmartInstitutions_ServiceRepositoryIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(SmartInstitutions_ServiceRepositoryIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see SmartIN_EventMessageACE.hh)
		cdr << idl_SmartIN_EventMessage;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_SmartIN_EventMessage);
		cdr << data_hash;
		// std::cout << "SmartIN_EventMessageCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void SmartIN_EventMessageCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		SmartInstitutions_ServiceRepositoryIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see SmartIN_EventMessageACE.hh)
		cdr >> idl_SmartIN_EventMessage;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_SmartIN_EventMessage);
		assert(data_hash == own_hash);
		// std::cout << "SmartIN_EventMessageCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace SmartInstitutions_ServiceRepository */
