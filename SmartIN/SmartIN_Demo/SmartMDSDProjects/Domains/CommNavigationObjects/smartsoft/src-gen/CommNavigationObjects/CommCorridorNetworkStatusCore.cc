//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommNavigationObjects/CommCorridorNetworkStatusCore.hh"

// serialization/deserialization operators
//#include "CommNavigationObjects/CommCorridorNetworkStatusACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommNavigationObjects 
{
	const char* CommCorridorNetworkStatusCore::getCompiledHash()
	{
		return CommNavigationObjectsIDL::REPO_HASH;
	}
	
	void CommCorridorNetworkStatusCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for CommNavigationObjects::CommCorridorNode(idl_CommCorridorNetworkStatus.nodes)
		CommNavigationObjects::CommCorridorNode::getAllHashValues(hashes);
	}
	
	void CommCorridorNetworkStatusCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "CommCorridorNetworkStatusCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for CommNavigationObjects::CommCorridorNode(idl_CommCorridorNetworkStatus.nodes)
		CommNavigationObjects::CommCorridorNode::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t CommCorridorNetworkStatusCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		std::vector<CommNavigationObjectsIDL::CommCorridorNode>::const_iterator data_nodesIt;
		for(data_nodesIt=data.nodes.begin(); data_nodesIt!=data.nodes.end(); data_nodesIt++) {
			seed += CommNavigationObjects::CommCorridorNode::generateDataHash(*data_nodesIt);
		}
		
		return seed;
	}
	#endif
	
	// default constructor
	CommCorridorNetworkStatusCore::CommCorridorNetworkStatusCore()
	:	idl_CommCorridorNetworkStatus()
	{  
		setNodes(std::vector<CommNavigationObjects::CommCorridorNode>());
	}
	
	CommCorridorNetworkStatusCore::CommCorridorNetworkStatusCore(const DATATYPE &data)
	:	idl_CommCorridorNetworkStatus(data)
	{  }

	CommCorridorNetworkStatusCore::~CommCorridorNetworkStatusCore()
	{  }
	
	void CommCorridorNetworkStatusCore::to_ostream(std::ostream &os) const
	{
	  os << "CommCorridorNetworkStatus(";
	  std::vector<CommNavigationObjects::CommCorridorNode>::const_iterator nodesIt;
	  std::vector<CommNavigationObjects::CommCorridorNode> nodesList = getNodesCopy();
	  for(nodesIt=nodesList.begin(); nodesIt!=nodesList.end(); nodesIt++) {
	  	nodesIt->to_ostream(os);
	  }
	  os << ") ";
	}
	
	// convert to xml stream
	void CommCorridorNetworkStatusCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		std::vector<CommNavigationObjects::CommCorridorNode>::const_iterator nodesIt;
		std::vector<CommNavigationObjects::CommCorridorNode> nodesList = getNodesCopy();
		counter = 0;
		os << indent << "<nodesList n=\"" << nodesList.size() << "\">";
		for(nodesIt=nodesList.begin(); nodesIt!=nodesList.end(); nodesIt++) {
			os << indent << "<nodes i=\"" << counter++ << "\">";
			nodesIt->to_xml(os, indent);
			os << indent << "</nodes>";
		}
		os << indent << "</nodesList>";
	}
	
	// restore from xml stream
	void CommCorridorNetworkStatusCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_nodesList("<nodesList n=\"");
		static const Smart::KnuthMorrisPratt kmp_nodes("\">");
		
		if(kmp_nodesList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			CommNavigationObjects::CommCorridorNode nodesItem;
			std::vector<CommNavigationObjects::CommCorridorNode> nodesList;
			kmp_nodes.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_nodes.search(is)) {
					nodesItem.from_xml(is);
					nodesList.push_back(nodesItem);
				}
			}
			setNodes(nodesList);
		}
	}
	
	/*
	void CommCorridorNetworkStatusCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommNavigationObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommNavigationObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see CommCorridorNetworkStatusACE.hh)
		cdr << idl_CommCorridorNetworkStatus;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_CommCorridorNetworkStatus);
		cdr << data_hash;
		// std::cout << "CommCorridorNetworkStatusCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void CommCorridorNetworkStatusCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommNavigationObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see CommCorridorNetworkStatusACE.hh)
		cdr >> idl_CommCorridorNetworkStatus;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_CommCorridorNetworkStatus);
		assert(data_hash == own_hash);
		// std::cout << "CommCorridorNetworkStatusCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommNavigationObjects */
