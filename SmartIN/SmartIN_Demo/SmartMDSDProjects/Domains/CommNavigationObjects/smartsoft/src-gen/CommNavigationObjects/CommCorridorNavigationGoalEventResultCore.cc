//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "CommNavigationObjects/CommCorridorNavigationGoalEventResultCore.hh"

// serialization/deserialization operators
//#include "CommNavigationObjects/CommCorridorNavigationGoalEventResultACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace CommNavigationObjects 
{
	const char* CommCorridorNavigationGoalEventResultCore::getCompiledHash()
	{
		return CommNavigationObjectsIDL::REPO_HASH;
	}
	
	void CommCorridorNavigationGoalEventResultCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for CommBasicObjects::CommPosition3d(idl_CommCorridorNavigationGoalEventResult.nextGoal)
		CommBasicObjects::CommPosition3d::getAllHashValues(hashes);
	}
	
	void CommCorridorNavigationGoalEventResultCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "CommCorridorNavigationGoalEventResultCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for CommBasicObjects::CommPosition3d(idl_CommCorridorNavigationGoalEventResult.nextGoal)
		CommBasicObjects::CommPosition3d::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t CommCorridorNavigationGoalEventResultCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		boost::hash_combine(seed, data.state);
		seed += CommBasicObjects::CommPosition3d::generateDataHash(data.nextGoal);
		
		return seed;
	}
	#endif
	
	// default constructor
	CommCorridorNavigationGoalEventResultCore::CommCorridorNavigationGoalEventResultCore()
	:	idl_CommCorridorNavigationGoalEventResult()
	{  
		setState(CommNavigationObjects::PathNavigationEventType());
		setNextGoal(CommBasicObjects::CommPosition3d());
	}
	
	CommCorridorNavigationGoalEventResultCore::CommCorridorNavigationGoalEventResultCore(const DATATYPE &data)
	:	idl_CommCorridorNavigationGoalEventResult(data)
	{  }

	CommCorridorNavigationGoalEventResultCore::~CommCorridorNavigationGoalEventResultCore()
	{  }
	
	void CommCorridorNavigationGoalEventResultCore::to_ostream(std::ostream &os) const
	{
	  os << "CommCorridorNavigationGoalEventResult(";
	  getState().to_ostream(os);
	  getNextGoal().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void CommCorridorNavigationGoalEventResultCore::to_xml(std::ostream &os, const std::string &indent) const {
		os << indent << "<state>";
		getState().to_xml(os, indent);
		os << indent << "</state>";
		os << indent << "<nextGoal>";
		getNextGoal().to_xml(os, indent);
		os << indent << "</nextGoal>";
	}
	
	// restore from xml stream
	void CommCorridorNavigationGoalEventResultCore::from_xml(std::istream &is) {
		static const Smart::KnuthMorrisPratt kmp_state("<state>");
		static const Smart::KnuthMorrisPratt kmp_nextGoal("<nextGoal>");
		
		if(kmp_state.search(is)) {
			CommNavigationObjects::PathNavigationEventType stateItem;
			stateItem.from_xml(is);
			setState(stateItem);
		}
		if(kmp_nextGoal.search(is)) {
			CommBasicObjects::CommPosition3d nextGoalItem;
			nextGoalItem.from_xml(is);
			setNextGoal(nextGoalItem);
		}
	}
	
	/*
	void CommCorridorNavigationGoalEventResultCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		CommNavigationObjectsIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(CommNavigationObjectsIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see CommCorridorNavigationGoalEventResultACE.hh)
		cdr << idl_CommCorridorNavigationGoalEventResult;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_CommCorridorNavigationGoalEventResult);
		cdr << data_hash;
		// std::cout << "CommCorridorNavigationGoalEventResultCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void CommCorridorNavigationGoalEventResultCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		CommNavigationObjectsIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see CommCorridorNavigationGoalEventResultACE.hh)
		cdr >> idl_CommCorridorNavigationGoalEventResult;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_CommCorridorNavigationGoalEventResult);
		assert(data_hash == own_hash);
		// std::cout << "CommCorridorNavigationGoalEventResultCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace CommNavigationObjects */
