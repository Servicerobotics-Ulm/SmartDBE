//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#include "SmartInstitutionsServiceRepository/SmartIN_EventStateTypeCore.hh"

// serialization/deserialization operators
//#include "SmartInstitutionsServiceRepository/SmartIN_EventStateTypeACE.hh"

// include the hash.idl containing the hash constant
#include "hash.hh"
#include <assert.h>
#include <cstring>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

#ifdef ENABLE_HASH
	#include <boost/functional/hash.hpp>
#endif

namespace SmartInstitutionsServiceRepository 
{
	const char* SmartIN_EventStateTypeCore::getCompiledHash()
	{
		return SmartInstitutionsServiceRepositoryIDL::REPO_HASH;
	}
	
	void SmartIN_EventStateTypeCore::getAllHashValues(std::list<std::string> &hashes)
	{
		// get own hash value
		hashes.push_back(getCompiledHash());
		// get hash value(s) for SmartInstitutionsServiceRepository::MemberIdentifier(idl_SmartIN_EventStateType.targets)
		SmartInstitutionsServiceRepository::MemberIdentifier::getAllHashValues(hashes);
		// get hash value(s) for SmartInstitutionsServiceRepository::SmartIN_EventMessage(idl_SmartIN_EventStateType.message)
		SmartInstitutionsServiceRepository::SmartIN_EventMessage::getAllHashValues(hashes);
	}
	
	void SmartIN_EventStateTypeCore::checkAllHashValues(std::list<std::string> &hashes)
	{
		// check own hash value
		if (strcmp(getCompiledHash(), hashes.front().c_str()) != 0)
		{
			std::cerr << "###################################################" << std::endl;
			std::cerr << "WARNING: HASHES OF COMMUNICATION OBJECTS MISSMATCH!" << std::endl;
			std::cerr << "SmartIN_EventStateTypeCore hash" << std::endl;
			std::cerr << "Expected: " << getCompiledHash() << std::endl;
			std::cerr << "Received: " << hashes.front() << std::endl;
			std::cerr << "###################################################" << std::endl;
		}
		assert(strcmp(getCompiledHash(), hashes.front().c_str()) == 0);
		hashes.pop_front();
		
		// check hash value(s) for SmartInstitutionsServiceRepository::MemberIdentifier(idl_SmartIN_EventStateType.targets)
		SmartInstitutionsServiceRepository::MemberIdentifier::checkAllHashValues(hashes);
		// check hash value(s) for SmartInstitutionsServiceRepository::SmartIN_EventMessage(idl_SmartIN_EventStateType.message)
		SmartInstitutionsServiceRepository::SmartIN_EventMessage::checkAllHashValues(hashes);
	}
	
	#ifdef ENABLE_HASH
	size_t SmartIN_EventStateTypeCore::generateDataHash(const DATATYPE &data)
	{
		size_t seed = 0;
		
		std::vector<SmartInstitutionsServiceRepositoryIDL::MemberIdentifier>::const_iterator data_targetsIt;
		for(data_targetsIt=data.targets.begin(); data_targetsIt!=data.targets.end(); data_targetsIt++) {
			seed += SmartInstitutionsServiceRepository::MemberIdentifier::generateDataHash(*data_targetsIt);
		}
		seed += SmartInstitutionsServiceRepository::SmartIN_EventMessage::generateDataHash(data.message);
		
		return seed;
	}
	#endif
	
	// default constructor
	SmartIN_EventStateTypeCore::SmartIN_EventStateTypeCore()
	:	idl_SmartIN_EventStateType()
	{  
		setTargets(std::vector<SmartInstitutionsServiceRepository::MemberIdentifier>());
		setMessage(SmartInstitutionsServiceRepository::SmartIN_EventMessage());
	}
	
	SmartIN_EventStateTypeCore::SmartIN_EventStateTypeCore(const DATATYPE &data)
	:	idl_SmartIN_EventStateType(data)
	{  }

	SmartIN_EventStateTypeCore::~SmartIN_EventStateTypeCore()
	{  }
	
	void SmartIN_EventStateTypeCore::to_ostream(std::ostream &os) const
	{
	  os << "SmartIN_EventStateType(";
	  std::vector<SmartInstitutionsServiceRepository::MemberIdentifier>::const_iterator targetsIt;
	  std::vector<SmartInstitutionsServiceRepository::MemberIdentifier> targetsList = getTargetsCopy();
	  for(targetsIt=targetsList.begin(); targetsIt!=targetsList.end(); targetsIt++) {
	  	targetsIt->to_ostream(os);
	  }
	  getMessage().to_ostream(os);
	  os << ") ";
	}
	
	// convert to xml stream
	void SmartIN_EventStateTypeCore::to_xml(std::ostream &os, const std::string &indent) const {
		size_t counter = 0;
		
		std::vector<SmartInstitutionsServiceRepository::MemberIdentifier>::const_iterator targetsIt;
		std::vector<SmartInstitutionsServiceRepository::MemberIdentifier> targetsList = getTargetsCopy();
		counter = 0;
		os << indent << "<targetsList n=\"" << targetsList.size() << "\">";
		for(targetsIt=targetsList.begin(); targetsIt!=targetsList.end(); targetsIt++) {
			os << indent << "<targets i=\"" << counter++ << "\">";
			targetsIt->to_xml(os, indent);
			os << indent << "</targets>";
		}
		os << indent << "</targetsList>";
		os << indent << "<message>";
		getMessage().to_xml(os, indent);
		os << indent << "</message>";
	}
	
	// restore from xml stream
	void SmartIN_EventStateTypeCore::from_xml(std::istream &is) {
		size_t counter = 0;
		
		static const Smart::KnuthMorrisPratt kmp_targetsList("<targetsList n=\"");
		static const Smart::KnuthMorrisPratt kmp_targets("\">");
		static const Smart::KnuthMorrisPratt kmp_message("<message>");
		
		if(kmp_targetsList.search(is)) {
			size_t numberElements;
			is >> numberElements;
			SmartInstitutionsServiceRepository::MemberIdentifier targetsItem;
			std::vector<SmartInstitutionsServiceRepository::MemberIdentifier> targetsList;
			kmp_targets.search(is);
			for(counter=0; counter<numberElements; counter++) {
				if(kmp_targets.search(is)) {
					targetsItem.from_xml(is);
					targetsList.push_back(targetsItem);
				}
			}
			setTargets(targetsList);
		}
		if(kmp_message.search(is)) {
			SmartInstitutionsServiceRepository::SmartIN_EventMessage messageItem;
			messageItem.from_xml(is);
			setMessage(messageItem);
		}
	}
	
	/*
	void SmartIN_EventStateTypeCore::get(ACE_Message_Block *&msg) const
	{
		// start with a default internal buffer size(will automatically grow if needed)
		ACE_OutputCDR cdr(ACE_DEFAULT_CDR_BUFSIZE);
		
		SmartInstitutionsServiceRepositoryIDL::HashList hashes;
		getAllHashValues(hashes);
		cdr << static_cast<ACE_CDR::Long>(hashes.size());
		for(SmartInstitutionsServiceRepositoryIDL::HashList::const_iterator it=hashes.begin(); it!=hashes.end(); it++)
		{
			cdr << ACE_CString(it->c_str());
		}
		
		// Here the actual serialization takes place using the OutputCDR serialization operator<<
		// (see SmartIN_EventStateTypeACE.hh)
		cdr << idl_SmartIN_EventStateType;
		
	#ifdef ENABLE_HASH
		ACE_CDR::ULong data_hash = generateDataHash(idl_SmartIN_EventStateType);
		cdr << data_hash;
		// std::cout << "SmartIN_EventStateTypeCore: current data hash: " << data_hash << std::endl;
	#endif
		
		// return a shallow copy of the serialized message 
		// (no data is actually copied, only the internal reference counter is incremented)
		// in order to prevent memory leaks the caller of this get(msg) method must
		// manually free the memory by calling the release() method of the message block msg
		msg = cdr.begin()->duplicate();
	}
	
	void SmartIN_EventStateTypeCore::set(const ACE_Message_Block *msg)
	{
		ACE_InputCDR cdr(msg);
	
		SmartInstitutionsServiceRepositoryIDL::HashList hashes;
		ACE_CDR::Long hashes_size;
		cdr >> hashes_size;
		for(int i=0; i<hashes_size; ++i) 
		{
			ACE_CString hash;
			cdr >> hash;
			hashes.push_back(hash.c_str());
		}
		checkAllHashValues(hashes);
		
		// Here the actual de-serialization takes place using the InputCDR serialization operator>>
		// (see SmartIN_EventStateTypeACE.hh)
		cdr >> idl_SmartIN_EventStateType;
		
	#ifdef ENABLE_HASH
		ACE_CDR::Long data_hash;
		cdr >> data_hash;
		ACE_CDR::Long own_hash = generateDataHash(idl_SmartIN_EventStateType);
		assert(data_hash == own_hash);
		// std::cout << "SmartIN_EventStateTypeCore: own data hash: " << own_hash << "; received data hash: " << data_hash << std::endl;
	#endif
	}
	*/
} /* namespace SmartInstitutionsServiceRepository */
