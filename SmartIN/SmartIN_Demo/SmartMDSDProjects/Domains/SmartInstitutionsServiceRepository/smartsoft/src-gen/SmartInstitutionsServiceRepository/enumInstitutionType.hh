//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//--------------------------------------------------------------------------
#ifndef SMARTINSTITUTIONSSERVICEREPOSITORY_INSTITUTIONTYPE_ENUM_H_
#define SMARTINSTITUTIONSSERVICEREPOSITORY_INSTITUTIONTYPE_ENUM_H_

#include <SmartInstitutionsServiceRepository/enumInstitutionTypeData.hh>

#include <string>
#include <iostream>

// SmartUtils used in from_xml method
#include "smartKnuthMorrisPratt.hh"

namespace SmartInstitutionsServiceRepository {
	
	class InstitutionType {
	private:
		SmartInstitutionsServiceRepositoryIDL::InstitutionType value;
		
	public:
		enum ENUM_InstitutionType {
			ENUM_VALUE_UNDEFINED = 0,
			TYPE0_USER_DEFINED = 1,
			TYPE1 = 2,
			TYPE2 = 3,
			TYPE3 = 4,
			TYPE4 = 5
		};
		
		// default constructor
		InstitutionType() { 
			value = ENUM_VALUE_UNDEFINED;
		}
		
		// copy constructor for enum type
		InstitutionType(ENUM_InstitutionType e) {
			value = static_cast<int>(e);
		}
		
		// copy constructor for IDL type
		InstitutionType(SmartInstitutionsServiceRepositoryIDL::InstitutionType e) {
			value = e;
		}
		
		//const SmartInstitutionsServiceRepositoryIDL::InstitutionType& get() const { return value; }
		
		// ENUM operator
		operator ENUM_InstitutionType() const {
			return static_cast<ENUM_InstitutionType>(value);
		}
		
		// compare operator
		bool operator == (const ENUM_InstitutionType t) const {
			return this->value == t;
		}
		
		std::string to_string() const {
			std::string result = "";
			switch (value) {
				case TYPE0_USER_DEFINED:
					result = "InstitutionType::TYPE0_USER_DEFINED";
					break;
				case TYPE1:
					result = "InstitutionType::TYPE1";
					break;
				case TYPE2:
					result = "InstitutionType::TYPE2";
					break;
				case TYPE3:
					result = "InstitutionType::TYPE3";
					break;
				case TYPE4:
					result = "InstitutionType::TYPE4";
					break;
				default:
					result = "ENUM_VALUE_UNDEFINED";
					break;
			};
			return result;
		}
		
		// helper method to easily implement output stream
		void to_ostream(std::ostream &os = std::cout) const {
			os << to_string();
		}
		
		// convert to xml stream
		void to_xml(std::ostream &os, const std::string &indent = "") const {
			os << indent << "<value>" << value << "</value>";
		}
		
		// restore from xml stream
		void from_xml(std::istream &is) {
			static const Smart::KnuthMorrisPratt kmp_value("<value>");
			if(kmp_value.search(is)) {
				is >> value;
			}
		}
	
	};
	
	inline std::ostream &operator<<(std::ostream &os, const InstitutionType &e)
	{
		e.to_ostream(os);
		return os;
	}
};

#endif
