//-----------------------------------------------------------------------------------
// Code generated by the Toolify Robotics GmbH extension for the SmartMDSD Toolchain 
// 
// Toolify Robotics GmbH
// Weinbergweg 216
// 89075 Ulm 
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//-----------------------------------------------------------------------------------

#include "CommVisualLocalizationFeatureMapJSON.hh"

#include "CommLocalizationObjectsJSON/CommVisualLocalizationFeatureJSON.hh"

namespace CommLocalizationObjectsIDL {

void to_json(const CommLocalizationObjectsIDL::CommVisualLocalizationFeatureMap& obj, nlohmann::json& j)
{
	// features: CommVisualLocalizationFeature[]
	for(size_t idx=0; idx < obj.features.size(); idx++) {
		nlohmann::json array_element;
		to_json(obj.features.at(idx), array_element);
		j["features"].emplace_back(array_element);
	}
	// isValid: Boolean
	j["isValid"] = obj.isValid;
}

/**
 * this transformation handles missing values and checks for correct types:
 * - missing primitive type values (including simple arrays) are set to zero values
 * - missing object type values are skipped entirely
 * - types are checked before performing the type-cast operation
 */
void from_json(const nlohmann::json& j, CommLocalizationObjectsIDL::CommVisualLocalizationFeatureMap& obj)
{
	// features: CommVisualLocalizationFeature[]
	if(j.contains("features") && j["features"].is_array()) {
		auto jfeatures = j["features"];
		obj.features.resize(jfeatures.size());
		for(size_t idx=0; idx < jfeatures.size(); idx++) {
			// convert the json array values individually
			if(jfeatures.at(idx).is_object()) {
				// from_json(jfeatures.at(idx), obj.features.at(idx));
				obj.features[idx] = jfeatures[idx].get<CommLocalizationObjectsIDL::CommVisualLocalizationFeature>();
			}
		}
	}
	// isValid: Boolean
	if(j.contains("isValid") && j["isValid"].is_boolean()) {
		obj.isValid = j["isValid"].get<bool>();
	}
}

} // end namespace CommLocalizationObjectsIDL
