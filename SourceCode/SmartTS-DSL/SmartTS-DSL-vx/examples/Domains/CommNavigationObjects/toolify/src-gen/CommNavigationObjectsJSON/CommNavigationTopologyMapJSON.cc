//-----------------------------------------------------------------------------------
// Code generated by the Toolify Robotics GmbH extension for the SmartMDSD Toolchain 
// 
// Toolify Robotics GmbH
// Weinbergweg 216
// 89075 Ulm 
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// Please do not modify this file. It will be re-generated
// running the code generator.
//-----------------------------------------------------------------------------------

#include "CommNavigationTopologyMapJSON.hh"

#include "CommNavigationObjectsJSON/CommNavigationTopologyPartJSON.hh"
#include "CommNavigationObjectsJSON/CommNavigationTopologyConnectionJSON.hh"

namespace CommNavigationObjectsIDL {

void to_json(const CommNavigationObjectsIDL::CommNavigationTopologyMap& obj, nlohmann::json& j)
{
	// isValid: Boolean
	j["isValid"] = obj.isValid;
	// vertices: CommNavigationTopologyPart[]
	for(size_t idx=0; idx < obj.vertices.size(); idx++) {
		nlohmann::json array_element;
		to_json(obj.vertices.at(idx), array_element);
		j["vertices"].emplace_back(array_element);
	}
	// edges: CommNavigationTopologyConnection[]
	for(size_t idx=0; idx < obj.edges.size(); idx++) {
		nlohmann::json array_element;
		to_json(obj.edges.at(idx), array_element);
		j["edges"].emplace_back(array_element);
	}
}

/**
 * this transformation handles missing values and checks for correct types:
 * - missing primitive type values (including simple arrays) are set to zero values
 * - missing object type values are skipped entirely
 * - types are checked before performing the type-cast operation
 */
void from_json(const nlohmann::json& j, CommNavigationObjectsIDL::CommNavigationTopologyMap& obj)
{
	// isValid: Boolean
	if(j.contains("isValid") && j["isValid"].is_boolean()) {
		obj.isValid = j["isValid"].get<bool>();
	}
	// vertices: CommNavigationTopologyPart[]
	if(j.contains("vertices") && j["vertices"].is_array()) {
		auto jvertices = j["vertices"];
		obj.vertices.resize(jvertices.size());
		for(size_t idx=0; idx < jvertices.size(); idx++) {
			// convert the json array values individually
			if(jvertices.at(idx).is_object()) {
				// from_json(jvertices.at(idx), obj.vertices.at(idx));
				obj.vertices[idx] = jvertices[idx].get<CommNavigationObjectsIDL::CommNavigationTopologyPart>();
			}
		}
	}
	// edges: CommNavigationTopologyConnection[]
	if(j.contains("edges") && j["edges"].is_array()) {
		auto jedges = j["edges"];
		obj.edges.resize(jedges.size());
		for(size_t idx=0; idx < jedges.size(); idx++) {
			// convert the json array values individually
			if(jedges.at(idx).is_object()) {
				// from_json(jedges.at(idx), obj.edges.at(idx));
				obj.edges[idx] = jedges[idx].get<CommNavigationObjectsIDL::CommNavigationTopologyConnection>();
			}
		}
	}
}

} // end namespace CommNavigationObjectsIDL
