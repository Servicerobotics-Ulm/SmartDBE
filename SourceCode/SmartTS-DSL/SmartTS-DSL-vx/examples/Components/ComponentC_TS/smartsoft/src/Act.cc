//--------------------------------------------------------------------------
// Code generated by the SmartSoft MDSD Toolchain
// The SmartSoft Toolchain has been developed by:
//  
// Service Robotics Research Center
// University of Applied Sciences Ulm
// Prittwitzstr. 10
// 89075 Ulm (Germany)
//
// Information about the SmartSoft MDSD Toolchain is available at:
// www.servicerobotik-ulm.de
//
// This file is generated once. Modify this file to your needs. 
// If you want the toolchain to re-generate this file, please 
// delete it before running the code generator.
//--------------------------------------------------------------------------
#include "Act.hh"
#include "ComponentC_TS.hh"

#include <iostream>

Act::Act(SmartACE::SmartComponent *comp) 
:	ActCore(comp)
{
	std::cout << "constructor Act\n";
}
Act::~Act() 
{
	std::cout << "destructor Act\n";
}


void Act::on_Push_Coordinate3D_Input_Port_sim(const DomainTSDemo::Coordinate3D &input)
{
	// upcall triggered from InputPort Push_Coordinate3D_Input_Port_sim
	// - use a local mutex here, because this upcal is called asynchroneously from outside of this task
	// - do not use longer blocking calls here since this upcall blocks the InputPort Push_Coordinate3D_Input_Port_sim
	// - if you need to implement a long-running procedure, do so within the on_execute() method and in
	//   there, use the method push_Coordinate3D_Input_Port_simGetUpdate(input) to get a copy of the input object
}
void Act::on_Send_Coordinate2D_Output_Port_ts(const DomainTSDemo::Coordinate2D &input)
{
	// upcall triggered from InputPort Send_Coordinate2D_Output_Port_ts
	// - use a local mutex here, because this upcal is called asynchroneously from outside of this task
	// - do not use longer blocking calls here since this upcall blocks the InputPort Send_Coordinate2D_Output_Port_ts
	// - if you need to implement a long-running procedure, do so within the on_execute() method and in
	//   there, use the method send_Coordinate2D_Output_Port_tsGetUpdate(input) to get a copy of the input object
}
int Act::on_entry()
{
	// do initialization procedures here, which are called once, each time the task is started
	// it is possible to return != 0 (e.g. when initialization fails) then the task is not executed further
	return 0;
}
int Act::on_execute()
{
	// this method is called from an outside loop,
	// hence, NEVER use an infinite loop (like "while(1)") here inside!!!
	// also do not use blocking calls which do not result from smartsoft kernel
	
	// to get the incoming data, use this methods:
	Smart::StatusCode status;
	DomainTSDemo::Coordinate3D push_Coordinate3D_Input_Port_simObject;
	status = this->push_Coordinate3D_Input_Port_simGetUpdate(push_Coordinate3D_Input_Port_simObject);
	if(status != Smart::SMART_OK) {
		std::cerr << status << std::endl;
		// return 0;
	} else {
		std::cout << "received: " << push_Coordinate3D_Input_Port_simObject << std::endl;
	}
	DomainTSDemo::Coordinate2D send_Coordinate2D_Output_Port_tsObject;
	status = this->send_Coordinate2D_Output_Port_tsGetUpdate(send_Coordinate2D_Output_Port_tsObject);
	if(status != Smart::SMART_OK) {
		std::cerr << status << std::endl;
		// return 0;
	} else {
		std::cout << "received: " << send_Coordinate2D_Output_Port_tsObject << std::endl;
	}

	std::cout << "Hello from Act " << std::endl;

	// it is possible to return != 0 (e.g. when the task detects errors), then the outer loop breaks and the task stops
	return 0;
}
int Act::on_exit()
{
	// use this method to clean-up resources which are initialized in on_entry() and needs to be freed before the on_execute() can be called again
	return 0;
}
